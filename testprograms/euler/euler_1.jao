/main()
{	
	Value i = 1;
	
	Value sum = 0;
	
	for(1 == 1; i < 1000; 1 == 1) ## Pardon the bad syntax; mostly a result of the underimplemented paren symbol
	{
		if(i % 3 == 0 || i % 5 == 0)
		{
			sum = sum + i;
		}
		
		i = i + 1;
	}

	return sum;
}