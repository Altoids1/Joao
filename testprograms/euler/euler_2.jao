/main()
{	
	Value a = 0;
	Value b = 1;
	Value c = 0;
	
	Value sum = 0;
	
	while(true)
	{
		c = a + b;
		
		if(c > 4_000_000)
		{
			break;
		}
		
		a = b;
		b = c;

		if(b % 2 == 0)
		{
			sum = sum + b;
		}
		
		Value dummy = print(a,b);
	}
	
	Value dummy = print("Sum:", sum );

	return 0;
}